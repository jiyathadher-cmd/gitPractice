const User =require('./userModel');
const Tour =require('./tourModel');
const Booking =require('./bookingModel');

module.exports = { User,Tour };

// Learn and Apply proper mongoose schema along their methods, hooks
// - Apply Middlewares for error handling and response handling
// -create a user and books CRUD from scratch apply middlewares,valiations,services,constants 